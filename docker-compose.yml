services:
  app:
    build: .
    container_name: ubnd_api
    environment:
      - PORT=8880
      - PREFIX_API=/api
    env_file:
      - .env
    ports:
      - "8880:8880"
    restart: unless-stopped
